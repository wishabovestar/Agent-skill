# 同步说明 — 2026-09-18 (睡眠/白日梦引擎 + 相关)

## 睡眠 / 白日梦
- `sleep_memory_v2.py` — ★ 新增诊断字段(**r1192**):
  `v2_scorable_n` / `v2_min_score` / `v2_below_discard_n` / `discard_at_cap` /
  `v2_discard_note`。**起因**:`v2_discard_rate` 为 0 有两种完全不同含义
  (「无低分条目」正常 vs 「判定链失效」异常),原指标**无法区分**。
  实测确认:09-15/09-16 两次为 0.42(修复生效),09-17 那次为 0 属**分布依赖**。
  ★ 新字段后一次运行即自洽:`below=6 / scorable=50 ⇒ 0.12`,`at_cap=False`。
- `dream_engine_health.py` — 两引擎四步体检(产物新鲜度 / A-B 指标 / discard 判定 / 噪声)
- `spindle_phase_check.py` — SPINDLE 相位检查

## 相关修复与基础设施
- `collect_hf.py` — ★ **r1192 修静默失效**:0 条目时**不再覆盖** `registry.total`,
  打印 🔴 并 **exit 1**(原来无条件 ✅ 且无退出码)。
- `curl_guarded.py` — ★ **保 `subprocess.run` 的 bytes/str 类型语义**。
  起因:早先版本无条件返回 str ⇒ 调用方 `r.stdout.decode()` 抛 AttributeError,
  且被 `except: continue` 吞掉 ⇒ **静默失效**。★ 教训:「字节数相同」≠ 行为等价。
- `verify_wired_types.py` — 隔离复测(subprocess,不 in-process import)
- `cron_replay.py` — cron 历史离线反事实评估
- `arxiv_tex.py` — 取 arXiv TeX 源码而非 PDF
- `scan_redirect_sinks_v5.py` — 重定向汇聚点扫描 v5

## ★ 本轮最重要的一条教训
**「字节数相同」不等于「行为等价」—— 类型是 drop-in 契约的一部分。**
r1186 我做 drop-in 等价性验证时只比了 `len(stdout)`,漏了 `type(stdout)`,
结果破坏了 5 个消费方,且因 `except Exception: continue` 而**静默**。
