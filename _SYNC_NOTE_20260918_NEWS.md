# 同步说明 — 2026-09-18 (新闻早报 / R1194)

## 核心修复:调度顺序死锁
Horizon 摘要在 **08:30** 生成, 而下游两个消费方都更早:
- `06:20` 巡天新闻采集(读 horizon 摘要作兜底源)
- `08:05` 每日新闻早报
⇒ ★ 两者【永远】拿不到当天摘要, 采集恒为 `status=partial`
   (`horizon: 今日摘要文件不存在`)。

🟢 实测证据: `horizon-2026-09-18-en.md` mtime = **08:32**;采集在 06:20 / 08:06。

**修法**: `92c2c8149fdb` `30 8` → `10 6`(提前到下游之前),
  并给采集加【多天回退】兜底(今日→昨日→前日)。

## 过滤判据:枚举黑名单 → 结构性判据
原判据是枚举式站点黑名单 ⇒ 遇到没枚举过的新站点必漏。
且 `len(title) < 12` 在真实数据上完全无效(实测 18 条长度 13–34)。
现改为三层结构性判据: 尾部站点标识 / 栏目导航词 / 栏目页形态(`新闻|标语`)。

## ★ 两个我引入并修掉的错误(值得记)
1. **写回条件 bug**: `if not dry and noise:` ⇒ `noise=0` 时【不写回】,
   纯标题清洗的结果会丢失。改为 `if not dry:`。
2. **清洗顺序错**: 先清洗再判噪声 ⇒ `"今日AI新闻| 每天看懂…"`
   被切掉 `"新闻"` 判别特征 ⇒ 本该拦的条目逃逸。
   **改为:先在原始标题上判噪声, 再清洗。**

★★ 通用教训: **过滤器与被清洗对象之间有顺序依赖** ——
清洗会改变判别特征, 判据必须在【原始形态】上施加。

## ★ 诚实边界
- 保留的 13 条中仍有 **3 条 2025 年旧文** + 大量"趋势/展望/盘点"聚合文
  ⇒ 这是【时效性/事件性】问题, **不是导航页噪声, 本轮未修**。
- **查询词可能是更上游的根因**(`"AI 最新突破 2026"` 天然返回聚合文), 未做 A/B。
- `horizon` 源在改动后**尚未跑过一次真实采集** ⇒
  "顺序死锁已解"是基于 mtime 的推断, **需明早 06:20 验证**。
- 清理脚本与采集器的判据是**两份拷贝** ⇒ 有漂移风险(无强制机制)。
